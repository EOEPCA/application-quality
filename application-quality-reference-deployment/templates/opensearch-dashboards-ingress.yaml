{{- if .Values.opensearch-dashboards.ingress.enabled -}}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "application-quality-reference-deployment.fullname" . }}-opensearch-dashboards
  labels:
    app.kubernetes.io/component: "opensearch-dashboards"
    {{- include "application-quality-reference-deployment.labels" . | nindent 4 }}
  {{- with .Values.opensearch-dashboards.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.opensearch-dashboards.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.opensearch-dashboards.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
   - host: {{ .Values.opensearch-dashboards.ingress.domain | quote }}
     http:
       paths:
          - path: /dashboards(/|$)(.*)
            pathType: Prefix
            backend:
              service:
                name: {{ include "application-quality-reference-deployment.fullname" . }}-opensearch-dashboards
                port:
                  number: {{ .Values.service.port }}
{{- end }}